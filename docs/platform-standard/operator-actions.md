# Operator Actions

Actions requiring credentials, vendor consoles, legal review, business
decisions, or production access — nothing here is a code task.

| Action ID | Section | What's needed | Why it's blocked from being a code fix | Requested by |
|---|---|---|---|---|
| OA-01 | 01 | Securities-law review of the Capital Partner / investment-programs feature (matching, introductions, NDA-gated offering materials) | Requires a licensed securities attorney's judgment, not engineering | R-02 |
| OA-02 | 01 | Confirm current CCPA/CPRA and other state privacy-law statutory thresholds against actual business metrics (revenue, records processed) | Requires business data this engineering pass has no visibility into, plus current-law verification | R-04 |
| OA-03 | 01 | Enable "require pull request review before merge" branch-protection rule on the default branch in GitHub repository settings | A GitHub repository setting, not a repo file | R-05 |
| OA-04 | 01 | *(Superseded — R-01 closed by encrypting the field in place rather than requiring a vendor migration decision.)* Optionally still consider migrating banking-info collection to Stripe Connect's own onboarding flow for other reasons (fewer PII fields Divini touches at all) | Architectural/vendor decision, not required to close R-01 | R-01 (closed) |
| OA-05 | 01 | Confirm SAQ A (or higher, if scope changes) PCI attestation is actually filed with the payment processor once Stripe goes live for real charges | Filing happens in Stripe's own dashboard/process, not this repo | Applicability register, PCI DSS row |
| OA-06 | 01 | Confirm who handles 1099-NEC issuance for Stripe Connect referral-partner payouts before the first live payout (Stripe itself can do this for Connect accounts, but it must be explicitly configured) | Tax/accounting decision + Stripe dashboard configuration | Applicability register, Information returns / 1099 row |
| OA-07 | 02 | *(Superseded — the AUP and DMCA notice-and-takedown text itself is now live in `Terms.tsx` Sections 10-11.)* | — | R-06 (partially closed) |
| OA-12 | 02 (gap closure) | Confirm `dmca@diviniprocure.com` is a real, monitored inbox, and register that address as the designated agent with the U.S. Copyright Office's online directory (a short form + a small fee) for full DMCA §512 safe-harbor eligibility | Requires real business contact info and a federal registration filing, not something this codebase can do on the owner's behalf | R-06 |
| OA-08 | 02 | Decide the retention period and legal-hold policy for each data category in `docs/platform-standard/data-retention-matrix.md`, then fund the engineering work to enforce it automatically | Business/legal policy decision that must precede the Section 06 implementation | R-08 |
| OA-09 | 03 | Enable "require pull request review before merge" on the default branch in GitHub repository settings (same underlying action as OA-03, restated here since Section 03 re-confirmed it) | GitHub repository setting | R-05, R-11 |
| OA-10 | 03 | Decide code-ownership boundaries and add a `CODEOWNERS` file if desired; decide a release-tagging convention (e.g. semver tags on deploy) | Process decision, not a safe unilateral engineering choice | R-11 |
| OA-11 | 03 | Select a SAST tool, license-checker, and SBOM generator appropriate to this stack and budget, then have engineering wire it into CI | Tooling/vendor decision | R-12 |
| OA-13 | 05 | Run a live Stripe test-mode smoke test of the subscription-cancellation flow (create a test subscription, cancel it, confirm the account keeps paid access until Stripe's test period boundary, confirm the webhook then correctly downgrades) before this path ships to production. **Now also covers verifying the RLS admin-escalation fix in the webhook handler (R-20)** - a real `checkout.session.completed` event must reach `/webhooks/stripe` and successfully write `subscription_entitlements` | Requires real Stripe test-mode credentials, not available in this engineering sandbox | R-16, R-20 |
| OA-14 | 06 | Install the nightly backup cron job on the production server (`0 3 * * * /root/sites/divini-procure/backup.sh ...` - see `AI_PROJECT_OS/23_DEPLOYMENT.md`'s new Backups & recovery section), and BEFORE relying on it, confirm the production DB user is exempt from Row-Level Security (`select rolsuper or rolbypassrls from pg_roles where rolname = current_user;` must return `t` - `backup.sh` fails loudly with this exact check if it isn't) | Requires production server/crontab access and a database credential check this sandbox cannot perform (no access to the real production database) | R-22 |
| OA-15 | 09 | Run a live Stripe test-mode smoke test of the payout release flow specifically: release a test payout, then force a retried request with the same underlying instruction (e.g. by simulating a timeout) and confirm Stripe's Idempotency-Key handling returns the original transfer rather than creating a second one | Requires real Stripe Connect test-mode credentials and a connected test payout account, not available in this engineering sandbox | R-26 |
