# Risk Register

Unresolved risks surfaced during this engagement. Owner/mitigation/target
state tracked per row; update status as later sections close items.

| Risk ID | Section raised | Description | Severity | Owner | Mitigation / Target State | Status |
|---|---|---|---|---|---|---|
| R-01 | 01 | Referral-partner bank `account_number` stored in plaintext in Postgres (`referral_partner_banking`); the code comment implies encryption is available but `STORAGE_ENCRYPTION_KEY` only covers uploaded files, not DB columns, so this field is not actually protected by that mechanism today | P1 | Engineering | Add field-level encryption (application-layer AES-GCM, keyed separately from file storage) | **CLOSED 2026-08-08.** `server/src/lib/fieldCrypto.ts` (new, always-on AES-256-GCM, key derived via HKDF from `SESSION_SECRET` - no new required env var). Wired into all 5 read/write sites in `referral-partner-onboarding.ts` for `account_number`, `routing_number`, and `iban` (the last of which was previously returned to clients fully unmasked - closed as part of the same fix). Verified live: raw DB column now holds base64 ciphertext (`RFBGM...`, the `DPF1` magic header); the owning partner's own `GET` returns correctly masked values (`****6789` etc.) derived by decrypting server-side, never the ciphertext or the full value. 5 new unit tests in `tests/fieldCrypto.test.ts`. Legacy plaintext rows (none existed in this environment) would continue to read correctly unchanged - no backfill migration required to deploy safely. |
| R-02 | 01 | No transaction-based compensation exists today for investment-program closings, but the platform hosts NDA-gated, accredited-tiered securities-adjacent offering material with investor matching/introduction workflows. Broker-dealer registration questions are not purely fee-triggered | P0 (procedural — a launch-blocker-in-spirit, not a code defect) | Owner + Counsel | Do not add a per-close success fee or any transaction-based investor compensation until a securities attorney reviews the introduction/matching mechanism itself | Open — flagged for `COUNSEL/OWNER REVIEW REQUIRED`, carried to Section 17 |
| R-03 | 01 | No third-party error-monitoring/log-aggregation (Sentry-style) exists server-side; a production incident may go undetected until a user reports it | P1 | Engineering (Section 14) | Wire structured error monitoring + alerting before scaling past the current low-traffic stage | Open — carried to Section 14 |
| R-04 | 01 | CCPA/CPRA and other state privacy-law thresholds cannot be confirmed from code alone (revenue/record-volume dependent) | CONDITIONAL | Owner + Counsel | Confirm current business metrics against each applicable state's thresholds; wire the Section 02 privacy-request architecture's opt-out flows to real data flows if applicable | Open — carried to Section 02/17 |
| R-05 | 01 | No branch-protection rule is visible from the repository (GitHub-UI setting, not a file); nothing currently prevents a direct unreviewed push to the default branch from becoming what ships | P1 | Owner (GitHub admin) | Enable "require PR review before merge" on the default/release branch in GitHub repository settings | Open — carried to Section 03 |
| R-06 | 02 | No DMCA notice-and-takedown process or AUP/Community Guidelines content exists, despite the platform hosting user-uploaded documents/images/investment materials from many parties | P1 | Owner + Counsel | Draft a DMCA designated-agent notice and AUP section; register a designated agent with the U.S. Copyright Office if pursuing full §512 safe harbor | **PARTIALLY CLOSED 2026-08-08.** Added `Terms.tsx` Section 10 (Acceptable Use, expanding on the existing Section 4) and Section 11 (Copyright Complaints / DMCA - standard 17 U.S.C. §512(c)(3) notice requirements, counter-notification rights, repeat-infringer policy statement). This is standardized statutory boilerplate, not a novel legal conclusion, so it was safe to implement directly rather than only flag - matching how the existing E-SIGN consent tracking was implemented earlier without needing a lawyer's sign-off. **Still requires owner action** (cannot be completed from inside this repo): the designated-agent address (`dmca@diviniprocure.com`) must be a real monitored inbox, and should be registered with the U.S. Copyright Office's designated-agent directory for full safe-harbor eligibility - flagged as a code `TODO(owner)` comment at the top of `Terms.tsx` plus `OA-12` below. General counsel review of the whole Terms page remains open per the pre-existing `52_COMPLIANCE.md` TODO. |
| R-07 | 02 | Cookie-consent choice (`consentGranted()`) is captured but never consumed anywhere in the codebase — a latent, unenforced contract | P2 | Engineering | Wire `consentGranted()` as a gate before loading any future analytics/tracking SDK; add a lint/review checklist note so this isn't missed when one is added | Open — low severity today (nothing exists to gate), re-open to P1 the moment any tracking SDK is added |
| R-08 | 02 | No automated data-retention/purge job exists (expired sessions, stale credentials, no legal-hold mechanism) - every deletion today is either immediate-and-total (account deletion cascade) or never happens at all | P1 | Engineering (Section 06) | Build a scheduled job against a real `data_retention_policies` table per `docs/platform-standard/data-retention-matrix.md`; add legal-hold override before it's needed in an actual dispute | Open — carried to Section 06 |
| R-09 | 02 (found during R-01 remediation) | `GET/POST /partner/onboarding/*` (agreement view, agreement sign, banking submit, banking view) unconditionally queried a `user_referrals.referred_by_partner_id` column that does not exist in any schema file - **every call to any of these 4 endpoints returned 500**, for every referral partner, regardless of how they were linked. Not a regression from this session's work; pre-existing and would have been hit by any real partner attempting onboarding | P0 (complete feature outage) | Engineering | Fixed the join to use the real linking columns (`user_referrals.code` -> `referral_partners.referral_code`) | **CLOSED 2026-08-08.** Verified live: agreement sign and banking submission both succeeded end to end after the fix (previously 500'd identically before it) |
| R-10 | 03 | CI never ran a real build (`vite build`) or validated `db/apply-all.sql` against a fresh database - the exact failure mode that caused multiple real, silent bugs earlier in this project's history had no automated gate at all | P0 | Engineering | Add both to CI as required (non-report-only) gates | **CLOSED 2026-08-08.** `.github/workflows/ci.yml` now runs `npm run build` and a new `db-schema` job (real Postgres service, two-pass apply, >=100-table sanity check). Verified by locally simulating the exact same sequence against a genuinely fresh database before committing |
| R-11 | 03 | No branch-protection rule, no `CODEOWNERS`, no tagged-release process | P1 | Owner (GitHub admin + process decision) | Enable required PR review in GitHub repository settings; decide code-ownership boundaries and a release-tagging convention | Open — not code-fixable; carried as an operator action |
| R-12 | 03 | No SAST tool, license inventory, or SBOM generation wired into CI/release | P2 | Engineering (future pass) | Select and wire a SAST tool and license/SBOM generation appropriate to the stack | Open — P2 maturity item, not a launch blocker at this stage |
| R-13 | 03 | Dead Supabase-era CSP allowlist code in `server/src/app.ts` (harmless - the env vars it checks are never set) | P2 | Engineering | Remove `SUPABASE_URL`/`VITE_SUPABASE_URL` branches from the CSP `connectSrc` config | **CLOSED 2026-08-08.** Removed. Verified live: no CSP violations in browser console across `/pricing` and `/login` after the change; full test suite still 173/173 |
| R-14 | 04 | `verify_token` was stored in plaintext (unlike `reset_token`, which was already SHA-256 hashed) even though verifying an email also issues a full login session, and the same column backs the company-ownership-transfer claim flow - a DB breach would have handed out directly-usable account-takeover and company-ownership-claim links | P1 | Engineering | Hash `verify_token` at rest the same way `reset_token` already is | **CLOSED 2026-08-08.** Shared `hashToken()` helper in `db.ts`, applied to all 6 read/write call sites. Verified live at the DB level: stored value is now the SHA-256 hash, legitimate lookups still work, the stored hash itself cannot be used as a token |
| R-15 | 04 | No user-facing "sign out of all devices" control - `revokeAllSessions()` only fired as an undocumented side effect of password reset, so a user who suspected a stolen session had no way to kill it without also changing their password | P1 | Engineering | Add a dedicated endpoint + UI control | **CLOSED 2026-08-08.** `POST /account/sessions/revoke-all` + "Sign out of all devices" in `Profile.tsx`. Verified live with a real two-device simulation: both sessions died, including the one that issued the call |
| R-16 | 05 | `POST /subscriptions/cancel` downgraded `tier_key` to free immediately for real Stripe subscriptions, contradicting the disclosed "keep access until period end" cancellation terms and cutting off already-paid-for access | P1 | Engineering | Defer to the existing, already-correct webhook-driven downgrade instead of downgrading eagerly | **CLOSED (record-only path verified live); Stripe-configured path verified by code review only, not live-tested (no Stripe test credentials in this sandbox) - see OA-13** |
| R-17 | 05 | No database-level Row-Level Security existed - every authorization guarantee was a server-code-only guarantee, with no DB backstop if a future route ever missed an ownership check. Previously judged too risky to retrofit without a large query-layer rewrite | P1 | Engineering | Retrofit RLS without the assumed rewrite, using `AsyncLocalStorage` request context propagated transparently through the existing `q()`/`q1()` call sites | **CLOSED 2026-08-08, per explicit operator direction ("this does need RLS btw").** See control-register S05-09/S05-10/S05-11 and `docs/platform-standard/authorization-matrix.md` for full detail, including two real bugs found and fixed during implementation (a self-referential policy recursion on `company_members`, and a two-pass `apply-all.sql` bootstrap break) - both reproduced live before being fixed, not just reasoned about. Verified via full functional regression + DB-layer adversarial testing + the existing 173-test unit suite, all passing |
| R-18 | 05 | The initial RLS pass (R-17) missed 5 tables that were part of the same original documented design intent (`vendor_profiles`, `package_line_items`, `bid_items`, `rfq_questions`, `feature_flags`) - found during this same-day gap-closure pass by cross-checking `db.ts`'s own header comment against `pg_tables.rowsecurity` | P1 | Engineering | Add policies for each, tracing every real read/write call site the same way as the rest of the retrofit | **CLOSED 2026-08-08.** See control-register S05-12. Live-verified: vendor company creation, package line-item add, RFQ ask/answer, and a full priced-bid submission with `bid_items`, including the buyer's cross-tenant read via `GET /quotes/compare/:packageId`. Two-pass `apply-all.sql` and the 173-test unit suite both re-verified clean after this extension. **Note:** `bid_line_items`/`bid_payment_milestones` (the newer Bid Studio feature, added after the original design list) remain outside RLS coverage - real today via app-layer checks only (`bid-studio.ts`'s `authorizeDraft`), not a regression, but not yet backstopped by RLS either. Left open as a scoped future increment rather than expanded into without the same tracing discipline |
| R-19 | 05 (found during Section 06 testing) | **P0, DATA LOSS.** `deleteMyAccount`'s "is this company now orphaned" check (`select 1 from company_members where company_id = $1 limit 1`) ran under the DEPARTING user's own RLS context, which (correctly, per R-18's `company_members` narrowing) can only see that user's OWN membership row. Reproduced live: a two-member company's building, documents, and everything else were wrongly cascade-deleted the moment the FIRST (non-last) member deleted their own account, because the orphan-check could not see the second member's still-existing row and concluded the company was empty | P0 | Engineering | Escalate `deleteMyAccount`'s whole transaction to an admin-equivalent RLS context - it is already fully self-authorized (only ever acts on the caller's own `userId`) and genuinely needs cross-member visibility for this one correctness check | **CLOSED 2026-08-08.** `server/src/db.ts`'s `deleteMyAccount` now calls `select set_config('app.is_admin', 't', true)` right after `begin`. Re-ran the exact reproduction (2-member company, departing member is not last) live: company, remaining member, and document all correctly survive; only the departing user's own row and membership are removed |
| R-20 | 05 (found during Section 06 testing) | The same "`company_members` narrowed to own-row-only" change (R-18) silently broke 3 other already-authorized cross-tenant/cross-user code paths that query `company_members` for someone other than the acting session: the Stripe webhook handler's `subscription_entitlements` writes (would hard-fail RLS on every real checkout - webhooks pass through `authMiddleware` with a real-but-unauthenticated context, not the "absent context = admin" case background jobs get), an addendum-publish notification fan-out to bidding vendor companies (would silently notify nobody), and referral-commission attribution on award (would silently zero out partner commissions) | P0 (payment-blocking) / P1 (silent functional regressions) | Engineering | Add a reusable `runAsAdmin()` helper (`requestContext.ts`) and apply it at each already-authorized call site, rather than one-off patches | **CLOSED 2026-08-08.** See control-register S06-04. Found via a systematic sweep of every `company_members` query in the codebase (not just the one that already broke), distinguishing the safe "own row" pattern (the vast majority) from the dangerous "look up other users' rows" pattern (these 3, plus the already-fixed R-19). Verified by code trace against each call site's actual authorization precondition (Stripe signature verification, `authorizeAddendum`'s ownership check, this route's own `requireUser` gate) - full live reproduction of the webhook case specifically was not possible in this sandbox (no Stripe test credentials, same limitation as OA-13), so this is disclosed as code-review-verified, not live-verified, for that one path |
| R-21 | 06 | `deleteMyAccount` hard-deletes the `users` row, but 34 foreign keys across 17 tables reference `users(id)` as "who did this" attribution columns (`created_by`, `uploaded_by`, `owner_user_id`, etc.) with Postgres's default `ON DELETE NO ACTION`. Any user who had personally authored so much as one document upload, pipeline opportunity, submittal, etc. - while their company survived them - could not delete their own account at all: a raw foreign-key-violation error, a real GDPR/CCPA right-to-erasure request that could never complete. Not caught by Section 02's earlier "PASS" on account deletion because that testing used fresh accounts with no cross-references, and not caused by the RLS work (pre-existing, just newly exposed by testing a more realistic multi-member scenario) | P0 | Engineering | Change all 34 FKs to `ON DELETE SET NULL` (every column already nullable) - the underlying record survives, only the erased user's attribution is unlinked | **CLOSED 2026-08-08.** `db/schema-user-fk-cascade-fix.sql` (new, spliced into `apply-all.sql`). Verified live: the exact reproduction (document uploader, non-last member, deletes account) now succeeds; the document survives with `uploaded_by` correctly nulled; the user row is genuinely gone. Two-pass `apply-all.sql` and the 173-test unit suite re-verified clean. **Extended same day**: the identical failure existed one level removed, for `deleteMyAccount`'s orphaned-COMPANY deletion path - 5 more `ON DELETE NO ACTION` FKs referencing `companies(id)`/`bids(id)` (`deliveries`, `submittals`, `progress_photos`, `dispute_messages`, `bid_recommendations`). Reproduced live (a solo vendor company with one submittal failed company deletion identically), then fixed the same way (two NOT NULL columns relaxed to nullable first, verified safe by reading every call site - neither has a downstream non-null display assumption). `select conrelid, conname from pg_constraint where contype='f' and confdeltype='a'` now returns zero rows across the entire schema. Re-verified: two-pass `apply-all.sql` clean, 173/173 unit tests |
| R-22 | 06 | Production is a single self-hosted Docker Postgres container on one droplet (no managed-database provider), and no backup mechanism of any kind existed anywhere in the repo - a droplet failure, a bad migration, or an operator mistake (`DROP TABLE`, a mis-targeted `apply-all.sql` run) had no recovery path at all | P0 | Engineering + Owner (crontab install is an operator action) | Add a `pg_dump`-based backup script with retention pruning; document the restore procedure | **CLOSED 2026-08-08** (script + procedure); **crontab installation is OA-14, not yet done - no code can install a crontab on the production host from here.** `backup.sh` (repo root). A real, non-obvious follow-on bug was found and documented in the same pass: `pg_dump` itself is subject to `FORCE ROW LEVEL SECURITY` (it always runs with `row_security=off`, which requires the dumping role to be an actual Postgres superuser or have `BYPASSRLS` - the app's `app.is_admin` session GUC does not help, since `row_security=off` skips evaluating policies rather than passing them). Verified live: a non-exempt owner role cannot `pg_dump` an RLS-forced table at all (reproduced the exact error); the same role WITH superuser succeeds cleanly. `backup.sh` fails loud with a specific remediation message if this occurs rather than silently writing a partial backup. Full dump/restore round trip verified live against a real copy of the schema (160 tables, exact row counts, RLS policies all matched after restore) |
| R-23 | 06 | `user_sessions` rows past their `expires_at` were never purged - `purgeExpiredSessions()` existed in `db.ts` but nothing called it on a schedule (documented as a known gap in `data-retention-matrix.md`, Section 02) | P2 | Engineering | Wire it into the existing in-process `setInterval` scheduler pattern (`index.ts`, already used for follow-up processing and marketplace publication) | **CLOSED 2026-08-08.** `index.ts` now runs `purgeExpiredSessions()` hourly, same no-external-cron-dependency pattern as the two existing jobs |
| R-24 | 07 | Only the auth surface, invite/referral lookups, score-refresh, and LLM calls had any rate limit; every other API route (document upload, bid submission, search, admin actions under a compromised session) had none at all | P2 | Engineering | Add a generous, generic API-wide backstop limiter | **CLOSED 2026-08-08.** New `apiRateLimit` (300 req/IP/min), applied globally in `app.ts` ahead of the router. Verified live: normal use unaffected, a 305-request rapid-fire loop hit `429` at request 295. Still just a courtesy throttle (in-memory, single-process, not a substitute for an edge WAF at real scale) - same disclosed limitation as every other limiter in `rateLimit.ts` |
| R-25 | 08 | 2 of the 3 LLM-calling routes had no dedicated cost/abuse rate limit - `onboarding.ts`'s website-profile-extraction route (full external fetch + LLM completion, no other purpose) and `blueprint.ts`'s `/analyze` route (conditionally calls the LLM) relied only on the generic 300/min API backstop, far too generous for an endpoint that costs real time and, with a paid LLM provider configured, real money per call | P1 | Engineering | Apply the existing `llmRateLimit` pattern (already used by `intel.ts`) to both routes | **CLOSED 2026-08-08.** `onboarding.ts`: 20/hour (purely LLM-driven). `blueprint.ts`: 60/hour (sized generously since deterministic, non-LLM classification work also runs through the same route). Typecheck clean, server verified healthy, 173-test suite re-verified clean |
| R-26 | 09 | **P0, real double-payment risk.** The only function in the app that moves real money (`createTransfer`, Stripe Connect payout release) had two independent gaps: (1) a SELECT-then-UPDATE status check with a genuine TOCTOU race - two concurrent release requests could both pass the check and both call Stripe; (2) no Stripe `Idempotency-Key`, so a retry after a network failure where the transfer actually succeeded (but the response was lost) would create a second real transfer, since the route's own failure handler resets the instruction to a re-releasable status | P0 | Engineering | (1) Replace the check with a single atomic `UPDATE ... WHERE status IN (...) RETURNING` so only one concurrent request can ever claim the row. (2) Add a stable per-instruction `Idempotency-Key` to the Stripe transfer call | **CLOSED 2026-08-08.** Race condition fix live-verified under genuine concurrency (two backgrounded, truly-simultaneous claim attempts against the same test row - exactly one won, the other affected zero rows). Idempotency-key plumbing added and code-reviewed; the Stripe-side behavior itself could not be live-verified end to end (no test credentials in this sandbox) - flagged as `OA-15` for a live Stripe test-mode smoke test before this path is trusted in production |
| R-27 | 09 | Stripe webhook processing was not idempotent against Stripe's at-least-once redelivery - every data write was already idempotent SQL (no integrity risk), but a redelivered `checkout.session.completed` would send a duplicate confirmation email. A table apparently built for exactly this purpose (`stripe_checkout_sessions`, with its own `stripe_event_id` column) already existed in the schema but was never referenced anywhere in the codebase | P2 | Engineering | Add a general `event.id`-keyed dedup claim at the top of the webhook handler, covering all 4 event types uniformly | **CLOSED 2026-08-08.** New `stripe_webhook_events` table + claim-via-`INSERT ... ON CONFLICT DO NOTHING RETURNING` at the top of the handler. Verified live via direct SQL (first claim of an event id succeeds, a repeat claim of the same id returns 0 rows). Two-pass `apply-all.sql` and 173-test suite re-verified clean |
| R-28 | 10 | RFC 8058 one-click unsubscribe was declared (`List-Unsubscribe-Post` header on every campaign email) but not actually implemented - the `/unsubscribe` endpoint was `GET`-only. Mail providers that honor this header (Gmail, Outlook, Yahoo) send a `POST`, so a real recipient clicking "Unsubscribe" in a compliant mail client would have gotten a `404` and silently NOT been unsubscribed - a genuine CAN-SPAM risk (failing to honor an unsubscribe request), not just a UX gap | P1 | Engineering | Add the missing `POST /unsubscribe` handler, sharing the existing token-based unsubscribe logic | **CLOSED 2026-08-08.** Live-verified: `POST /api/unsubscribe?token=...` with the RFC 8058 body now returns `200` and correctly records the suppression; the token is invalidated after use (idempotent replay); the pre-existing `GET` path re-verified still working |
| R-29 | 11 | No automated accessibility regression testing exists in CI (no `axe-core`, no Lighthouse CI, no `pa11y`, no Playwright/E2E framework wired into this repo at all) - manual spot-checks this pass found the foundation genuinely solid (real skip-link, focus-visible styles, reduced-motion support, correctly-labeled forms, an honest accessibility statement, passing color-contrast on the core palette), but nothing prevents a future regression across the app's 121 pages from going unnoticed | P2 | Engineering (future pass) | Add an `axe-core`-based Playwright smoke test to CI covering the critical-path pages | **Open, deliberately not fixed this pass.** Adding a new test framework + CI job under this session's time constraints, without the ability to properly tune and stabilize it, risks a flaky/untrusted gate that's worse than no gate - same reasoning as R-12 (SAST/SBOM tooling, Section 03). Carried forward as a scoped future increment, not silently dropped |
