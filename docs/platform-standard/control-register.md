# Control Register

One row per control across all ALFY2 sections. Append-only within a section;
do not delete a prior section's rows when starting a new one (per Section 00
cross-section continuity rule). Re-test and update `Last Checked` when a
control's status changes.

| Control ID | Section | Requirement | Priority | Applicability | Status | Evidence | Risk | Remediation | Validation | Owner/Action | Last Checked |
|---|---|---|---|---|---|---|---|---|---|---|---|
| S01-01 | 01 | Architecture/data-flow map exists and is accurate | P1 | Required | PASS | `docs/platform-standard/architecture-map.md`, cross-checked against `AI_PROJECT_OS/04,20,21,22,23,24` and direct code inspection of `server/src/config.ts`, `pool.ts`, `stripe.ts`, `stripe-connect.ts` | Low | — | Read against live `server/src/*` this session | — | 2026-08-08 |
| S01-02 | 01 | Actor/capability inventory covers every real persona | P1 | Required | PASS | `applicability-register.md` actor table; confirmed no guardian/minor, health, education, or background-check actor exists via `grep` across `db/schema-*.sql` | Low | — | Grep for `create table` matches against expected regulated-actor schemas returned none | — | 2026-08-08 |
| S01-03 | 01 | Data classification inventory | P1 | Required | PASS | `applicability-register.md` data table | Medium | Referral-partner banking `account_number` stored in cleartext — tracked as S06 finding, not fixed in this section (out of Section 01 scope; Section 01 is discovery, not remediation of a Section 06-owned control) | Confirmed by reading `db/schema-referral-partner-onboarding.sql` directly | Section 06 owner | 2026-08-08 |
| S01-04 | 01 | Regulatory applicability matrix populated with evidence, not assumption | P1 | Required | PASS | `applicability-register.md` regime table, 30 regimes assessed, each with an evidence citation | Low | Securities-law item is a genuine open question, not a defect — flagged for counsel, not "fixed" | N/A — applicability determination, not a testable code control | `COUNSEL/OWNER REVIEW REQUIRED` on securities-law item and multi-state privacy thresholds | 2026-08-08 |
| S01-05 | 01 | Certification/attestation classification, HIPAA correctly NOT treated as a certification | P1 | Required | PASS | `applicability-register.md` certification table; HIPAA is absent from that table by design (it is a legal-compliance question, assessed separately in the regime table as N/A) | Low | — | — | — | 2026-08-08 |
| S01-06 | 01 | Output gate: later sections tagged REQUIRED/CONDITIONAL/N/A | P1 | Required | PASS | `applicability-register.md` output-gate table | Low | — | — | — | 2026-08-08 |
| S02-01 | 02 | Legal document inventory: existence, routing, accessibility | P1 | Required | PASS | Terms, Privacy, Cookies, Accessibility, PaymentPolicy, NonCircumvention, MessagingPolicy all exist as routed pages (`src/pages/*.tsx`); linked from `CookieBanner.tsx` and cross-referenced within `Terms.tsx` | Low | — | Grep for the page files + route wiring in `src/App.tsx` | — | 2026-08-08 |
| S02-02 | 02 | AUP / Community Guidelines / DMCA notice-and-takedown content | P1 | Required | **FAIL** | No dedicated page or section found; `grep -in "DMCA\|takedown\|acceptable use\|community guideline" src/pages/Terms.tsx` returned nothing | Medium — platform hosts UGC (project docs/images, investment materials); no formal DMCA §512 safe-harbor notice-and-takedown process reduces safe-harbor eligibility if infringing content is ever uploaded | Draft a DMCA designated-agent notice + AUP section (legal content, not structural code — flagged for owner/counsel rather than authored unilaterally in this pass, consistent with rule 12) | Not applicable until drafted | `COUNSEL/OWNER REVIEW REQUIRED` to draft and publish | 2026-08-08 |
| S02-03 | 02 | Consent/versioning architecture: acceptance is durably recorded per document + version, not just the latest overwritten | P1 | Required | **PARTIAL → upgraded to PASS this pass** | Before: `users.terms_agreed_at/terms_version/consent_ip` only ever held the most recent acceptance (an UPDATE, not an append). After: new `user_legal_acceptances` table (`db/schema-consent-and-audit.sql`) plus `db.recordLegalAcceptance()`, wired into registration (`terms`) and vendor onboarding (`vendor_agreement`) | Low (post-fix) | Implemented this pass | Live: fresh vendor registration produced both rows with correct `document_type`/`version`/`source`/`ip_address` — see evidence register | — | 2026-08-08 |
| S02-04 | 02 | Vendor Agreement acceptance is server-verified, not only client-gated | P0 (server-authority rule) | Required | **FAIL → FIXED this pass** | Before: `POST /companies` never checked or stored vendor-agreement acceptance; only the client disabled a button. After: `server/src/lib/company-validation.ts` (`validateCompanyCreation`, pure function) rejects `kind: 'vendor'` without `vendorAgreementAccepted: true` with `400` | Low (post-fix) | Implemented this pass | Automated: `tests/company-validation.test.ts` (5 new tests, 168/168 suite passes). Live: direct `curl POST /companies` without the field → 400; with `vendorAgreementAccepted: true` → 201 | — | 2026-08-08 |
| S02-05 | 02 | Privacy-request architecture: export/correct/delete | P1 | Required | PASS | `POST /account/delete`, `GET /account/export` (`server/src/routes.ts:275-308`); `server/src/db.ts:515-618` `deleteMyAccount`/`exportMyData`, both scoped strictly to the caller's own `userId`/`companyIds`, export redacts secret columns | Low | — | Read implementation directly; confirmed scoping logic (no target-user parameter accepted on delete) | — | 2026-08-08 |
| S02-06 | 02 | Deletion revokes sessions/prevents further login | P1 | Required | PASS | `user_sessions.user_id references users(id) on delete cascade` (`db/schema-sessions.sql:7`); `auth.ts` `verify()` checks `isSessionActive(jti)` against `user_sessions` on every request | Low | — | Traced the cascade + the per-request revocation check in code | — | 2026-08-08 |
| S02-07 | 02 | Data retention matrix documented | P1 | Required | PASS (documentation only — no automated enforcement job exists) | `docs/platform-standard/data-retention-matrix.md` | Medium | No automated purge job for expired sessions/stale credentials/legal holds — carried as a gap, not fixed (would be a Section 06 implementation, not a Section 02 documentation task) | — | Section 06 owner (automated retention enforcement) | 2026-08-08 |
| S02-08 | 02 | Cookie/tracking consent: banner exists, choice enforced against real trackers | P2 | Required | PARTIAL | `CookieBanner.tsx` exists, stores a real choice (`procure_consent_v1`) with timestamp, links Cookie/Privacy policies. `consentGranted()` is exported but **never imported/called anywhere else in the codebase** | Low today (no third-party analytics/ad SDK exists to gate — confirmed in Section 01's architecture map), but the contract is unenforced and would silently fail to gate a tracker added later without a code reviewer noticing | Not fixed this pass (nothing to gate today; fixing "nothing" would be inventing a control with no code to attach it to) | — | Re-check whenever an analytics/tracking SDK is added — must call `consentGranted()` before firing | 2026-08-08 |
| S02-09 | 02 | Marketing opt-out suppresses delivery | CONDITIONAL | Required | UNKNOWN | Not evaluated in this section | — | — | — | Section 10 owner | 2026-08-08 |
| S02-10 | 02 (gap closure) | Referral-partner banking PII encrypted at rest | P1 | Required | **FAIL → FIXED** | `server/src/lib/fieldCrypto.ts` (new); wired into `server/src/routes/referral-partner-onboarding.ts` (5 sites) | Low (post-fix) | Implemented | 6 new unit tests (`tests/fieldCrypto.test.ts`); live: DB column confirmed ciphertext, API confirmed masked-not-raw | — | 2026-08-08 |
| S02-11 | 02 (gap closure) | Referral-partner onboarding endpoints functional | P0 | Required | **FAIL → FIXED** | `server/src/routes/referral-partner-onboarding.ts` - broken join on a nonexistent column fixed in all 5 occurrences | Low (post-fix) | Implemented | Live: agreement sign + banking submit both succeeded post-fix (500 before) | — | 2026-08-08 |
| S03-01 | 03 | Repository governance: branch protection, CODEOWNERS, tagged releases | P1 | Required | **FAIL (documentation gaps, correctly not code-fixable)** | No branch-protection rule visible (GitHub setting); no `CODEOWNERS` file; `git tag` returns nothing (no release/versioning process); `package.json` version is a static, never-bumped `0.1.0` in both root and server | Medium | Not fixable from inside the repo for branch protection (GitHub UI setting); a `CODEOWNERS` file and a tagging convention are addable, but are process decisions for the owner to define (who owns what), not something to invent unilaterally | — | `OPERATOR ACTION REQUIRED` (see operator-actions.md) | 2026-08-08 |
| S03-02 | 03 | Environment separation: fail-closed prod config, environment identity guard | P1 | Required | **PARTIAL → PASS this pass** | Already strong: `config.ts` throws at startup in prod if `SESSION_SECRET`/`DOWNLOAD_URL_SECRET`/`DATABASE_URL` are unset/default; `ALLOWED_ORIGINS` empty denies cross-origin. Added this pass: a startup log in `index.ts` printing `environment=` and the DB host/name (never credentials) so a deploy operator immediately sees which environment/database a process bound to | Low (post-fix) | Implemented this pass | Live: server restart log shows `[divini-procure] environment=development db_host=localhost db_name=divini_procure` | — | 2026-08-08 |
| S03-03 | 03 | Secrets inventory | P1 | Required | PASS (inventory only, no values printed) | 55 distinct `process.env.X` references catalogued across `server/src/**`; see evidence register for the full table. All secret-shaped vars (`SESSION_SECRET`, `DOWNLOAD_URL_SECRET`, `DATABASE_URL`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `EMAIL_API_KEY`, `S3_SECRET_ACCESS_KEY`, `LLM_API_KEY`, `PAYPAL_SECRET`, `STORAGE_ENCRYPTION_KEY`, `APS_CLIENT_SECRET`) are read from `process.env` only, never hardcoded, never logged | Low | — | `grep -rohE "process\.env\.[A-Z_0-9]+"` across `server/src/` | — | 2026-08-08 |
| S03-04 | 03 | No secrets committed to git history or current tree | P1 | Required | PASS | `.gitignore` covers `.env`, `.env.*`, `*.pem`, `*.key`, `serviceAccount*.json`; `git ls-files` for those patterns returns nothing; `git check-ignore -v server/.env` confirms this session's own local test `.env` (with real-looking dev secrets) was never trackable; `git log --all -p` grepped for live Stripe/AWS/Google key-shaped patterns (`sk_live_`, `AKIA[0-9A-Z]{16}`, `AIza...`, private-key PEM headers) found only documentation references to variable *names*, never values | Low | — | Commands above run against this session's actual clone | — | 2026-08-08 |
| S03-05 | 03 | Dependency/supply-chain controls | P1/P2 | Required | PARTIAL | PASS: lockfiles committed for both `package.json` and `server/package.json`; `.github/dependabot.yml` opens weekly update PRs; `npm audit --omit=dev` runs every CI push (report-only, with a documented reason - see `52_COMPLIANCE.md`). GAP (P2): no SAST tool wired; no license inventory/checker; no SBOM generation for releases; no formal abandoned-dependency review process (found one concrete instance - see S03-06) | Low-Medium | SAST/license/SBOM tooling not added this pass - genuine new tooling decisions, not a safe drop-in fix, appropriate for a P2 maturity pass rather than this section | — | Owner decision on which SAST/SBOM tool to standardize on | 2026-08-08 |
| S03-06 | 03 | Dead/vestigial config: Supabase CSP allowlist | P2 | N/A (informational) | PARTIAL | `server/src/app.ts:65-74` still branches on `SUPABASE_URL`/`VITE_SUPABASE_URL` to build the CSP `connectSrc` allowlist - a leftover from the pre-native-auth Supabase era. Confirmed dead: those env vars are never set in any documented environment (`24_ENVIRONMENTS.md` already flags this), and `grep -rl "supabase" src/` (frontend) returns nothing. Currently harmless (the conditional spreads are no-ops when unset) | Low | Not removed this pass - cosmetic/tech-debt cleanup, not a defect, and touching CSP code deserves its own isolated, carefully-tested change rather than a drive-by edit inside a different section | — | P2 cleanup candidate for a future pass | 2026-08-08 |
| S03-07 | 03 | CI gates: build step, migration validation | P0/P1 | Required | **FAIL → FIXED this pass** | Before: CI ran typecheck (`tsc --noEmit`, doesn't catch real `vite build` bundling failures) and tests, but never ran an actual build, and had zero validation that `db/apply-all.sql` still bootstraps cleanly from scratch - despite that exact failure mode having caused real, repeated bugs earlier in this project's history (see `AI_PROJECT_OS/13_CHANGELOG.md`). After: added a "Build SPA" step (`npm run build`, the real `tsc && vite build`) to the existing job, and a new required `db-schema` job that spins up a real Postgres 16 service container, applies `db/apply-all.sql` **twice** (matching the documented production bootstrap procedure exactly), and fails the build (`ON_ERROR_STOP=1` plus a >=100-table sanity check) if it doesn't come up clean | Low (post-fix) | Implemented this pass | Live-simulated locally before committing: created a genuinely fresh Postgres database, ran the exact same two-pass `psql -v ON_ERROR_STOP=1 -f db/apply-all.sql` sequence CI now runs, confirmed 0 errors both passes and 160 tables (well above the 100-table gate) | — | 2026-08-08 |
| S03-08 | 03 (gap closure) | Remove dead Supabase CSP code | P2 | N/A | **FIXED** | `server/src/app.ts` `connectSrc` no longer branches on `SUPABASE_URL`/`VITE_SUPABASE_URL` | Low | Implemented | Live: no CSP-related console messages across `/pricing`, `/login`; full suite 173/173 | — | 2026-08-08 |
| S02-12 | 02 (gap closure) | AUP and DMCA notice-and-takedown content published | P1 | Required | **FAIL → FIXED** | `src/pages/Terms.tsx` Sections 10 (Acceptable Use) and 11 (Copyright Complaints / DMCA), standard §512(c)(3) statutory boilerplate | Low | Implemented; designated-agent registration remains owner-only (`TODO(owner)` comment + `OA-12`) | Live: page renders correctly, renumbered sections 12-14 confirmed via screenshot; `npm run build` succeeds | Owner: register designated agent with US Copyright Office | 2026-08-08 |
| S04-01 | 04 | Account model: one canonical identity per human, not conflated with org/role | P1 | Required | PASS | `users` (identity) vs `company_members` (org membership + role) are cleanly separate tables; no OAuth/social identities to conflict-check | Low | — | Read schema + code directly | — | 2026-08-08 |
| S04-02 | 04 | Password hashing | P1 | Required | PASS | `server/src/lib/passwordHash.ts` - scrypt, 16-byte random salt, `timingSafeEqual` comparison, plaintext never logged | Low | — | Prior-session security audit already confirmed this; re-verified against current code | — | 2026-08-08 |
| S04-03 | 04 | Session cookie attributes | P1 | Required | PASS | `server/src/routes/auth-native.ts` `setSessionCookie`/`clearSessionCookie`: `HttpOnly`, `SameSite=Lax`, `Secure` in production | Low | — | Read directly | — | 2026-08-08 |
| S04-04 | 04 | Server-side session revocation (not just JWT expiry) | P1 | Required | PASS | `user_sessions` table + `isSessionActive(jti)` checked on every request in `auth.ts` `verify()`; logout deletes the row; already confirmed independently in Section 02 | Low | — | Live-verified this pass with a two-device sign-out-all-devices test (see S04-06) | — | 2026-08-08 |
| S04-05 | 04 | Verification and reset tokens: high entropy, expiring, single-use (replay resistant), hashed at rest | P1 | Required | **PARTIAL → FIXED this pass** | Tokens are `randomBytes(32)` hex (256 bits) and cleared from the DB on use for both flows (already true). **Gap found and fixed**: `reset_token` was SHA-256 hashed before storage but `verify_token` was stored in plaintext - and verifying an email also issues a full login session (`handleVerify` → `issueSessionResponse`), and the same column backs the company-ownership-transfer "claim" flow, so a leaked plaintext verify_token was equivalent in severity to a leaked reset token, not lower. Added a shared `hashToken()` in `server/src/db.ts`, applied to all read/write sites for both token types | Low (post-fix) | Implemented this pass | Live: a direct DB-level script confirmed the stored value now equals `sha256(rawToken)`, not the raw token; the legitimate lookup by raw token still succeeds; attempting to use the stored hash itself as a token fails. Full suite 173/173 | — | 2026-08-08 |
| S04-06 | 04 | Logout: single device and all devices | P1 | Required | **PARTIAL → FIXED this pass** | Single-device logout already existed and worked (`POST /auth/logout` revokes that session's `jti`). "All devices" only fired as an undocumented side effect of password reset - no direct, user-facing way to do it. Added `POST /account/sessions/revoke-all` + a "Sign out of all devices" control in `Profile.tsx` | Low (post-fix) | Implemented this pass | Live: registered one account, logged in from two separate cookie jars (simulated devices), confirmed both returned `200` from `/auth/me`, called the new endpoint from device 1, confirmed **both** devices then returned `401` | — | 2026-08-08 |
| S04-07 | 04 | Account enumeration resistance | P1 | Required | PASS | Login returns one generic `"Incorrect email or password."` for both wrong-password and no-such-account; `/auth/forgot` and `/auth/resend-verification` always return `{ok:true}` regardless of whether the account exists | Low | — | Read directly | — | 2026-08-08 |
| S04-08 | 04 | Rate limiting on auth endpoints | P1 | Required | PASS | `loginRateLimit`, `registerRateLimit`, `forgotRateLimit`, `resendVerifyRateLimit`, `resetPasswordRateLimit` each applied to their respective route; already functionally exercised (429s observed) in an earlier phase of this engagement | Low | Minor P2 observation: `/auth/verify` itself has no dedicated rate limiter, mitigated by 256-bit token entropy making brute force infeasible regardless | — | — | 2026-08-08 |
| S04-09 | 04 | CORS + cookie credentials cannot be abused cross-origin (CSRF posture) | P1 | Required | PASS | `cors({credentials: true, origin: ...})` denies any origin not on the explicit allowlist in production (empty allowlist = deny all cross-origin), not a wildcard-plus-credentials misconfiguration; combined with `SameSite=Lax` this is the modern-practice CSRF mitigation (no separate CSRF token exists, and none is required given this configuration) | Low | — | Read directly | — | 2026-08-08 |
| S04-10 | 04 | Client cannot mint its own role/admin authority | P1 | Required | PASS | Admin status derives server-side from `ADMIN_ALLOWED_EMAILS` only, checked fresh on every request; never trusted from client input or baked into the SPA bundle - already independently confirmed in prior work this session | Low | — | — | — | 2026-08-08 |
| S04-11 | 04 | OAuth/social login, MFA/2FA, recovery codes | N/A | N/A | N/A | None of these are offered - native email/password is the only auth method | N/A | Not applicable to build; a future business decision, not a gap in what exists today | N/A | N/A | 2026-08-08 |
| S04-12 | 04 | Mobile-specific auth (Sign in with Apple, etc.) | N/A | N/A | N/A | No OAuth exists in the mobile Capacitor shell either (it loads the same web auth); N/A follows directly from S04-11 | N/A | — | N/A | N/A | 2026-08-08 |
| S05-01 | 05 | Authorization model documented (roles, tenancy, visibility, no RLS) | P0 | Required | PASS | `docs/platform-standard/authorization-matrix.md` | Low | — | Reuses `docs/SECURITY-PRIVACY.md` + confirmed via live adversarial testing below | — | 2026-08-08 |
| S05-02 | 05 | Cross-tenant write protection | P0 | Required | PASS | `POST /buildings/:id/packages` cross-tenant attempt | Low | — | Live: `403 "not the owner of this building"` | — | 2026-08-08 |
| S05-03 | 05 | Cross-tenant document access (list + signed-URL issuance) | P0 | Required | PASS | `GET /documents`, `GET /documents/signed-url` | Low | — | Live: both `403` for an unrelated company, matching the code's own documented IDOR-fix comment | — | 2026-08-08 |
| S05-04 | 05 | Signed download URL integrity (tamper + expiry resistance) | P0 | Required | PASS | `GET /documents/download` HMAC signature + `exp` | Low | — | Live: bit-flipped signature → `403`; expired timestamp → `403`; valid link works regardless of caller (correct presigned-URL semantics, not a flaw) | — | 2026-08-08 |
| S05-05 | 05 | Admin route protection + forged-admin-flag resistance | P0 | Required | PASS | `GET /admin/subscriptions`; admin status resolution | Low | — | Live: non-admin session → `403 forbidden`; `isAdmin` in a request body is never read anywhere server-side | — | 2026-08-08 |
| S05-06 | 05 | Cross-tenant read on marketplace-discovery resources (buildings/packages) | P0 | Required | **PASS (confirmed intentional, not a defect)** | `GET /buildings/:id` returns full data for a non-owning company | N/A | This matches the documented, deliberate "read any, write own" design carried over from the original Supabase RLS policies (`21_DATABASE.md`) | Live-confirmed the behavior; cross-checked against design docs before concluding it's intentional rather than assuming | — | 2026-08-08 |
| S05-07 | 05 | Admin impersonation / view-as controls | N/A | N/A | N/A | Feature does not exist (`grep -rln "impersonat"` found nothing relevant) | N/A | Not applicable to build; no capability to misuse | N/A | N/A | 2026-08-08 |
| S05-08 | 05 | Cancelled subscription does not retain (or lose) entitlement incorrectly | P1 | Required | **FAIL → FIXED this pass** | `POST /subscriptions/cancel` was downgrading `tier_key` immediately even for real Stripe subscriptions, contradicting the UI's own "keep access until period end" promise and the already-correct webhook-driven downgrade path (`customer.subscription.deleted`/`.updated`) | Low (post-fix); one path unverified live - see below | Now defers to the existing webhook for real subscriptions; only downgrades immediately when there's no live subscription to defer to | Live-verified the record-only (no-Stripe) branch end to end (immediate downgrade, correct). **The new Stripe-configured deferred branch could not be live-tested - no Stripe test credentials in this sandbox** - verified by code reading only. Flagged as `OA-13` for a pre-production Stripe test-mode smoke test | Owner: Stripe test-mode smoke test before production | 2026-08-08 |
